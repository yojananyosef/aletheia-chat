# Tasks — aletheia-pendientes

Backlog sin orden fijo; cada unidad se ejecuta end-to-end (cambios → validaciones →
openspec → commits) siguiendo el flujo estándar del repo.

- [x] 1. SSR del contenido: catálogo de la home prerenderizado en server + primeros mensajes del capítulo en HTML (islas client para interacción). Meta: el texto bíblico visible sin JS. Hecho: `bibleServer.loadChapterStatic` + `ChapterPrerender` (hasta 1ª pausa, mín 5 / máx 15) + `ChapterPrerenderGate` + `initialData` en `useBibleChat`/`ChatView`; verificado en `.next/server/app/*.html` y `e2e/ssr.spec.ts` (9/9 e2e).
- [x] 2. A11y avanzada: `aria-live="polite"` en el feed + skip-link + contraste AA + `OptionsMenu` dialog. Hecho: feed `role="log" aria-live="polite"` (anuncia mensajes nuevos/typing), `SkipLink` en root layout con `#contenido` en mains de home/chat, `text-gray-400/500` → `text-gray-600` (12 archivos, 0 restos), `OptionsMenu` `role="dialog"` + foco inicial + `aria-haspopup="dialog"` y retorno de foco en `ChatHeader` (Escape ya existía; sin `aria-modal`: popover sin backdrop ni trap); verificado en `e2e/a11y.spec.ts` (4/4).
- [x] 3. PWA offline: service worker vanilla (`public/sw.js`) con cache-first para `public/data/**` y app-shell. Hecho: `ServiceWorkerRegister` (solo prod, en dev con `NEXT_PUBLIC_SW=1`) + `manifest.ts` (`dir`, `categories`) + `env NEXT_PUBLIC_SW` en `playwright.config.ts`; verificado en `e2e/offline.spec.ts` (HTML + JSON sin red) y README.
- [x] 4. Slug `apocalipsis` (hoy `revelation`): renombrar en `books.ts` + `public/data/` + redirects 301 en `next.config.js`. Hecho: `books.ts` + `CODE_BY_SLUG` + key de `participants.json` renombrados (código gateway sigue `REV`; `public/data/` no tenía dir `revelation`); redirects 308 `/revelation/:chapter`→`/apocalipsis/:chapter` y `/revelation`→`/` verificados contra prod.
- [x] 5. NT completo: ingesta de los 23 libros restantes con el flujo por unidad (heurística → LLM review → sweep → unlock → e2e). Hecho 2026-09-21: canon 66/66 libros, 1.189 capítulos (SpaRVG); commits `caf73a5` (1 Pedro) → `6a38e5b` (Apocalipsis, cierra el canon); `qa-sweep` full 0 duros, `run.test.mjs` 55/55.
